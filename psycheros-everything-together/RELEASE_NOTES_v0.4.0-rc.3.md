# Everything Together 0.4.0-rc.3

- Restores More Uploads inside the combined Psycheros 0.11.2 source bridge:
  multiple images, documents, and audio files now coexist with Expression
  Sprites and Screen Presence in ordinary chat and Yin Yang typed voice.
- Resolves the shared chat composer, server template, voice-send, and service
  worker seams without dropping either component's behavior.
- Gives the combined client payload a fresh cache identity so existing RC2
  installs refresh their browser and WebView assets.
- Keeps Accessibility Controls 0.1.0-rc.2, Windows Shell Fix 0.3.0-rc.2, and
  HTF Music Listener 0.3.0-rc.2 unchanged.
- Makes the attachment source tests independent of the caller's working
  directory and adds combined-suite cache assertions.
