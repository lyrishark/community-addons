# Windows Shell Fix 0.3.0-rc.2

- Verified the API-v1 replacement tool against Psycheros 0.11.
- Expanded compatibility to Psycheros `>=0.10.0 <0.12.0`.
- Confirmed stock 0.11 still invokes `sh -c`, so the Windows fix remains needed.

The execution and redaction behavior is unchanged from RC1.
